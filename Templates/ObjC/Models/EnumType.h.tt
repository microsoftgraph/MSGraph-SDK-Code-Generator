<#@ template debug="true" hostspecific="true" language="C#" #>
<#@ include file="SharedObjC.template.tt"#>
<#
var entity = host.CurrentType.AsOdcmEnum();
#>

#include <Foundation/Foundation.h>

typedef NS_ENUM(NSInteger, <#=GetObjCTypeIdentifier(entity)#>){

<#
var lastMember = entity.Members.LastOrDefault();
foreach(var member in entity.Members)
{
    var value = GetObjCEnumMember(entity,member) + (lastMember == member ? string.Empty : ",");

#>
	<#=value#>
<#

}

#>

};
